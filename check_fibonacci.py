n = int(input('enter a no to be checked whether fibonacci: '))
if is_fibonacci(n) 
def is_fibonacci(n):
    phi = 0.5 + 0.5 * math.sqrt(5.0)
    a = phi * n
    returnor  n == 0 or abs(round(a) - a) < 1.0 / n
